<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Welcome to Ranked!</title>
  <style>
    body {
      font-family: 'Arial', sans-serif;
      background-color: #e0f7fa; /* Light sky blue */
      color: #333;
      margin: 0;
      padding: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }

    h1 {
      font-size: 3em;
      color: #0277bd; /* Sky Blue */
      margin-bottom: 20px;
    }

    .banner {
      font-size: 1.3em;
      color: #0288d1; /* Darker Sky Blue */
      margin-bottom: 40px;
    }

    .login-container {
      background-color: #fff; /* White background */
      border-radius: 12px;
      box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
      padding: 40px;
      width: 320px;
      text-align: center;
    }

    .login-container h2 {
      color: #0277bd; /* Sky Blue */
      font-size: 2em;
      margin-bottom: 20px;
    }

    .login-container input {
      width: 100%;
      padding: 12px;
      margin: 10px 0;
      border: 1px solid #b3e5fc; /* Light blue border */
      border-radius: 8px;
      font-size: 1em;
      color: #333;
    }

    .login-container button {
      width: 100%;
      padding: 12px;
      background-color: #0288d1; /* Sky Blue */
      color: white;
      border: none;
      border-radius: 8px;
      font-size: 1.2em;
      cursor: pointer;
      transition: background-color 0.3s;
    }

    .login-container button:hover {
      background-color: #0277bd; /* Darker sky blue on hover */
    }

    .login-container p {
      font-size: 1em;
      color: #555;
      margin-top: 20px;
    }

    .login-container a {
      color: #0288d1; /* Sky blue link */
      text-decoration: none;
      font-weight: bold;
    }

    .login-container a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>

  <div class="login-container">
    <h1>Welcome to Ranked!</h1>
    <div class="banner">Create and Share Your Favorite Lists</div>

    <h2>Login to Get Started</h2>

    <form action="/login" method="post">
      <input type="text" name="username" placeholder="test_user" required>
      <input type="password" name="password" placeholder="qwerty" required>
      <button type="submit">Login</button>
    </form>

    <p>Don't have an account? <a href="#">Sign Up</a></p>
  </div>

</body>
</html>
